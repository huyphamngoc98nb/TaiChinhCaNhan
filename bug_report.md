# Mobile QA Test Report & Bug Logs

**Thời gian chạy test:** 10:01:31 29/5/2026
**Tổng số test cases:** 16
**Đạt (PASS):** 0 / 16
**Lỗi phát hiện (FAIL):** 16

## 🛑 Kết quả: FAIL (Phát hiện lỗi)

Dưới đây là danh sách chi tiết các bug được ghi nhận kèm mức độ ưu tiên:

### Bug #1: iPhone 12 Layout and UX (iPhone 12)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone 12.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-88640-orm-iPhone-12-Layout-and-UX-iPhone-12/test-failed-1.png)

---

### Bug #2: iPhone 12 Layout and UX (iPhone X)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone X.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-88640-orm-iPhone-12-Layout-and-UX-iPhone-X/test-failed-1.png)

---

### Bug #3: iPhone 12 Layout and UX (Pixel 5)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Pixel 5.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-88640-orm-iPhone-12-Layout-and-UX-Pixel-5/test-failed-1.png)

---

### Bug #4: iPhone 12 Layout and UX (Tablet (iPad))
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Tablet (iPad).
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-88640-orm-iPhone-12-Layout-and-UX-Tablet-iPad-/test-failed-1.png)

---

### Bug #5: iPhone X Layout and UX (iPhone 12)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone 12.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-f3d70-Form-iPhone-X-Layout-and-UX-iPhone-12/test-failed-1.png)

---

### Bug #6: iPhone X Layout and UX (iPhone X)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone X.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-f3d70-Form-iPhone-X-Layout-and-UX-iPhone-X/test-failed-1.png)

---

### Bug #7: iPhone X Layout and UX (Pixel 5)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Pixel 5.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-f3d70-Form-iPhone-X-Layout-and-UX-Pixel-5/test-failed-1.png)

---

### Bug #8: iPhone X Layout and UX (Tablet (iPad))
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Tablet (iPad).
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: expect(locator).toBeVisible() failed`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-f3d70-Form-iPhone-X-Layout-and-UX-Tablet-iPad-/test-failed-1.png)

---

### Bug #9: Pixel 5 Layout and UX (iPhone 12)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone 12.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-fc991--Form-Pixel-5-Layout-and-UX-iPhone-12/test-failed-1.png)

---

### Bug #10: Pixel 5 Layout and UX (iPhone X)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone X.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-fc991--Form-Pixel-5-Layout-and-UX-iPhone-X/test-failed-1.png)

---

### Bug #11: Pixel 5 Layout and UX (Pixel 5)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Pixel 5.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-fc991--Form-Pixel-5-Layout-and-UX-Pixel-5/test-failed-1.png)

---

### Bug #12: Pixel 5 Layout and UX (Tablet (iPad))
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Tablet (iPad).
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: expect(locator).toBeVisible() failed`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-fc991--Form-Pixel-5-Layout-and-UX-Tablet-iPad-/test-failed-1.png)

---

### Bug #13: Tablet (iPad) Layout and UX (iPhone 12)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone 12.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: expect(locator).toBeVisible() failed`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-2486a-m-Tablet-iPad-Layout-and-UX-iPhone-12/test-failed-1.png)

---

### Bug #14: Tablet (iPad) Layout and UX (iPhone X)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: iPhone X.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-2486a-m-Tablet-iPad-Layout-and-UX-iPhone-X/test-failed-1.png)

---

### Bug #15: Tablet (iPad) Layout and UX (Pixel 5)
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Pixel 5.
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: locator.fill: Error: strict mode violation: locator('form input[type="text"]') resolved to 2 elements:`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-2486a-m-Tablet-iPad-Layout-and-UX-Pixel-5/test-failed-1.png)

---

### Bug #16: Tablet (iPad) Layout and UX (Tablet (iPad))
- **Mức độ nghiêm trọng (Severity):** **P2**
- **Các bước tái hiện (Steps to reproduce):**
  1. Thiết lập giả lập thiết bị: Tablet (iPad).
  2. Truy cập màn hình thêm giao dịch tại `/transactions/new`.
  3. Thực hiện các thao tác nhập liệu và cuộn trang.
- **Kết quả thực tế (Actual result):**
  `Error: expect(locator).toBeVisible() failed`
- **Kết quả kỳ vọng (Expected result):**
  - Vùng nội dung form phải được cuộn độc lập, không bị đè hay che khuất bởi vùng sticky header.
  - Nút Lưu (Save) hoặc các trường nhập liệu không bị che bởi bàn phím ảo hay bị cắt mất.
- **Ảnh chụp lỗi (Screenshot):** [Xem ảnh chụp màn hình](file:///D:/Personal Project/TaiXiuCaNhan/test-results/src-tests-add-transaction.-2486a-m-Tablet-iPad-Layout-and-UX-Tablet-iPad-/test-failed-1.png)

---

